package Media;

/**
 * Contains all the available sfx resources
 */
public enum EAudio {
    ost, button_click,
    round_start, death_sound,
    small_food, large_food,
    ghost_moving, ghost_vulnerable, ghost_vulnerable_end, ghost_ate,
    
    placeholder
}
